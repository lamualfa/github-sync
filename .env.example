# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=ghp_your_github_token_here

# GitHub repository information
# These should match the repository that contains the submodules
GITHUB_OWNER=your-username
GITHUB_REPO=your-repo-name

# Repository configuration
# If REPO_PATH is not provided or doesn't exist, repository will be cloned to temporary folder
# If REPO_PATH is provided, that local repository will be used

# Optional: Local repository path
# REPO_PATH=/path/to/your/repository

# Optional: Repository branch to clone (defaults to main)
# REPO_BRANCH=main

# Optional: Update interval in minutes (defaults to 15)
INTERVAL_MINUTES=15

# Optional: Run once and exit (useful for testing)
RUN_ONCE=false

# Optional: Git identity configuration
# These are used when committing submodule updates
# If not provided, defaults will be used
# GIT_USER_NAME=Your Name
# GIT_USER_EMAIL=your.email@example.com